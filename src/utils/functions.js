const images = () => {
  fetch('https://example.com/api/endpoint')
  .then((response) => {
    // procesar la respuesta del servidor
  })
  .catch((error) => {
    // manejar cualquier error
  });
};
